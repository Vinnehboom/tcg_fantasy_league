<% flash.each do |key, value| %>
  <div class="alert flash-bar alert-<%= flash_key_mapping(key) %>"
       role="alert"
       data-controller="removals"
       data-action="animationend->removals#remove">
    <%= raw value %>
  </div>
<% end %>