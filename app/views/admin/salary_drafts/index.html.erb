<% if current_user && policy(:salary_draft).new? %>
  <div class="d-flex p-2 m-2 w-100 justify-content-end">
    <%= link_to new_admin_salary_draft_path, class: 'btn btn-success' do %>
      <%= t(".add") %>
    <% end %>
  </div>
<% end %>
<%= render partial: "table", locals: { salary_drafts: @salary_drafts } %>